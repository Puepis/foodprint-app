import 'package:flutter/material.dart';

class ManualSearchPage extends StatelessWidget {
  const ManualSearchPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
